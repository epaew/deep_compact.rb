# frozen_string_literal: true

RSpec.describe DeepCompact do
  it 'has a version number' do
    expect(DeepCompact::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
